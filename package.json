{
  "name": "trustmcp",
  "private": true,
  "version": "0.1.0",
  "description": "TrustMCP — an agent-first, open trust-center standard and reference network.",
  "license": "FSL-1.1-ALv2",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "spec:validate": "node packages/trustmcp-spec/scripts/validate-examples.mjs"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.3"
  }
}
